import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartdailyplanner.app',
  appName: 'Smart Daily Planner',
  webDir: 'out'
};

export default config;
